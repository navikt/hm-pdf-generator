<style>
    @page {
        margin: 64pt 64pt 74pt 64pt;

        @bottom-right {
            font-family: 'Source Sans 3', sans-serif;
            font-size: 9pt;
            font-weight: normal;
            line-height: 16pt;
            content: 'side ' counter(page) ' av ' counter(pages);
        }

        @bottom-left {
            font-family: 'Source Sans 3', sans-serif;
            font-size: 9pt;
            font-weight: normal;
            line-height: 16pt;
            content: '{{bunntekst}}';
            margin: 0;
        }
    }
</style>
<style>
    html {
        font-family: 'Source Sans 3', sans-serif;
        font-size: 11pt;
        font-weight: normal;
        line-height: 16pt;
    }

    body {
        margin: 0;
    }

    h1 {
        font-size: 16pt;
        font-weight: bold;
        letter-spacing: 0.03em;
        line-height: 20pt;
        margin: 0 0 26pt 0;
        page-break-after: avoid;
    }

    h2 {
        font-size: 13pt;
        font-weight: bold;
        letter-spacing: 0.025em;
        margin: 0 0 6pt 0;
        page-break-after: avoid;
    }

    h3 {
        font-size: 12pt;
        font-weight: bold;
        letter-spacing: 0.02em;
        margin: 0 0 6pt 0;
        page-break-after: avoid;
    }

    h4 {
        font-size: 11pt;
        font-weight: bold;
        letter-spacing: 0.01em;
        margin: 0 0 6pt 0;
        page-break-after: avoid;
    }

    p {
        margin: 0 0 16pt 0;
    }

    section {
        margin: 0 0 26pt 0;
    }

    /* Fikser noen rare greier med at marginer stackes på toppen av hverandre ved page-breaks */
    section p:last-child {
        margin-bottom: 0;
    }

    .header {
        position: relative;
        margin-bottom: 48pt;
    }

    .header svg {
        height: 16pt;
        margin-bottom: 48pt;
    }

    .header dl dd {
        display: inline;
        margin: 0;
    }

    .header dl dd:after {
        display: block;
        content: '';
    }

    .header dl dt {
        display: inline-block;
        min-width: 160px;
    }

    .header span {
        display: block;
        position: absolute;
        right: 0;
        bottom: 0;
    }

    section.signatur {
        margin-top: 32pt;
        page-break-inside: avoid;
    }

    ul, ol {
        margin: -16pt 0 16pt 0;
        padding-left: 2em;
    }

    a {
        color: #000000;
        white-space: nowrap;
    }

    .merkepenn {
        background: yellowgreen;
    }
</style>